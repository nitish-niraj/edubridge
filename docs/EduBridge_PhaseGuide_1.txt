
EduBridge
Connecting Retired Teachers with Students

Phase-by-Phase Development Guide
Complete AI Agent Build Instructions

Tech Stack: Laravel 10 (PHP 8.2) • Vue.js 3 • MySQL 8 • Pusher • Twilio • Razorpay
7 Phases  |  10 Months  |  Academic Project 2025


Project Overview & How to Use This Guide
This document provides granular, AI-agent-ready instructions for building EduBridge — a web platform connecting retired teachers with students seeking personalized tutoring. Every phase is self-contained and can be handed directly to an AI coding agent.

How to Use This Document
• Each phase has a GOAL, DURATION, TECH CONTEXT, and a TASK TABLE.
• The task table gives every individual component with a full description column — this is what you paste to an AI agent.
• Phases must be completed in order. Each phase depends on the previous phase's output.
• All backend is Laravel 10 (PHP 8.2). All frontend uses Vue.js 3 embedded in Laravel Blade templates.
• Database: MySQL 8. Real-time: Pusher + Laravel Echo. Video: Twilio. Payments: Razorpay.

Global Technical Standards (Apply to ALL Phases)
• Project name: edubridge. Laravel project lives at /var/www/edubridge on the server.
• All PHP must be PSR-12 compliant. All Vue components use the Composition API (setup()).
• Every API route must return JSON. Use Laravel API Resources for all responses.
• All DB migrations must use up() and down() methods properly.
• Every controller method must have Form Request validation — no raw $request->input() without validation.
• Soft deletes (SoftDeletes trait) on all major tables: users, bookings, messages.
• All timestamps in UTC in the database. Display conversion happens on the frontend.
• Use .env for all secrets — never hardcode API keys.
• Write a Feature Test for every major API endpoint using PHPUnit.

Database Naming Conventions
• Table names: snake_case plural (teacher_profiles, booking_slots).
• Foreign keys: table_singular_id pattern (teacher_id, student_id, booking_id).
• JSON columns: use MySQL JSON type for arrays like subjects[], languages[].
• Enum columns: use string with check constraints via Laravel enum cast.


PHASE 1: Foundation — Laravel Setup, DB Schema, Authentication & Profiles
Duration: Months 1–2  |  Goal: Get the skeleton of the app running with working login and profile pages
Phase 1 Overview
This is the most critical phase. Every other phase depends on a clean, well-structured foundation. The AI agent must set up the entire Laravel project from scratch, configure the database, build the role-based authentication system, and create working profile pages for both Teachers and Students. By the end of Phase 1, a user should be able to register as either a student or a teacher, verify their email, log in, and fill out their complete profile.

Phase 1 — Environment & Stack
• PHP 8.2, Composer, Laravel 10, Node.js 18+ (for Vite/npm)
• MySQL 8 database: edubridge_db
• Laravel Breeze (Blade + Vue starter kit) for authentication scaffolding
• Spatie Laravel-Permission package for role management
• Intervention Image package for profile photo resizing/upload
• Laravel Telescope (dev only) for debugging

Phase 1 — Task Breakdown
Task / ComponentDetailed Description for AI AgentFiles / Artifacts1.1 Laravel Project SetupRun: composer create-project laravel/laravel edubridge. Configure .env with DB credentials (DB_DATABASE=edubridge_db, DB_USERNAME, DB_PASSWORD). Set APP_NAME=EduBridge, APP_URL, APP_TIMEZONE=UTC. Install Laravel Breeze: composer require laravel/breeze --dev then php artisan breeze:install vue. Run npm install && npm run build. Confirm welcome page loads.composer.json, .env, routes/web.php1.2 Database Schema — Core TablesCreate migrations in this order: (1) users table — id, name, email, phone (nullable, unique), password, role ENUM('student','teacher','admin'), email_verified_at, phone_verified_at, avatar (nullable), status ENUM('active','suspended','pending') default pending, remember_token, timestamps, softDeletes. (2) teacher_profiles — id, user_id (FK ? users.id, cascade delete), bio TEXT nullable, experience_years INT default 0, hourly_rate DECIMAL(8,2) nullable, is_free BOOLEAN default false, is_verified BOOLEAN default false, rating_avg DECIMAL(3,2) default 0.00, total_reviews INT default 0, subjects JSON nullable, languages JSON nullable, gender ENUM('male','female','other') nullable, timestamps. (3) student_profiles — id, user_id (FK), class_grade VARCHAR(20) nullable, school_name VARCHAR(150) nullable, subjects_needed JSON nullable, preferred_language VARCHAR(50) nullable, timestamps.database/migrations/*.php1.3 Eloquent ModelsUser model: add HasRoles (Spatie), SoftDeletes traits. Add relationships: hasOne TeacherProfile, hasOne StudentProfile. Add role helper methods: isTeacher(), isStudent(), isAdmin(). TeacherProfile model: belongsTo User. Cast subjects and languages as array (JSON cast). StudentProfile model: belongsTo User. Cast subjects_needed as array.app/Models/User.php, TeacherProfile.php, StudentProfile.php1.4 Role-Based Auth with SpatieInstall: composer require spatie/laravel-permission. Publish and run migrations. In AuthServiceProvider (or AppServiceProvider on Laravel 10), register roles. Seed three roles in DatabaseSeeder: student, teacher, admin. Create a RoleMiddleware that redirects users to their correct dashboard based on role. Register this middleware in bootstrap/app.php as 'role'.app/Http/Middleware/RoleMiddleware.php, database/seeders/1.5 Registration FlowsCreate two separate registration pages: /register/student and /register/teacher. Student registration form fields: name, email, phone, password, password_confirm, class_grade, school_name. Teacher registration form fields: name, email, phone, password, gender, bio (short). On form submit, create user with correct role, create the corresponding profile record (teacher_profiles or student_profiles), assign Spatie role, send email verification. Do NOT use a single form — keep them completely separate with separate controllers: StudentAuthController and TeacherAuthController.app/Http/Controllers/Auth/, resources/views/auth/1.6 Email OTP VerificationAfter registration, generate a 6-digit numeric OTP. Store it in a verifications table: id, user_id, otp (hashed), type ENUM('email','phone'), expires_at, used_at, timestamps. Send OTP via email using a mailable class OtpMail. Show a /verify-otp page where user enters the 6 digits. On correct OTP entry, set email_verified_at, update user status to active. OTP expires in 15 minutes. Allow resend after 60 seconds. Invalidate old OTPs when a new one is generated.app/Mail/OtpMail.php, verifications migration, VerifyOtpController1.7 Social Login (Google) for StudentsInstall Laravel Socialite: composer require laravel/socialite. Add Google OAuth credentials to .env: GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, GOOGLE_REDIRECT. Create SocialiteController with redirectToGoogle() and handleGoogleCallback() methods. On callback, find-or-create user by email, assign student role, auto-create student_profile record, mark email_verified_at as now(). Only allow Google login for students — block if the email already exists as a teacher account.app/Http/Controllers/SocialiteController.php, config/services.php1.8 Password ResetUse Laravel's built-in password reset via email link (already included in Breeze). Ensure mail is configured in .env (use Mailtrap for dev). Customize the reset email template to match EduBridge branding. Test the full forgot ? email ? reset ? login flow.resources/views/emails/reset-password.blade.php1.9 Teacher Profile PageCreate /teacher/profile route (auth + role:teacher middleware). Show a multi-step profile completion form using Vue 3 components: Step 1 — Bio, experience years, gender. Step 2 — Subjects (multi-select checkboxes: Math, Science, English, History, Geography, Physics, Chemistry, Biology, Hindi, Punjabi, Computer Science, Economics, Commerce, Other). Step 3 — Languages they teach in (multi-select: Hindi, Punjabi, English, Urdu, Other). Step 4 — Hourly rate (number input) or toggle 'I offer free/volunteer sessions'. Step 5 — Profile photo upload (max 2MB, resize to 300x300 using Intervention Image, store to public/avatars/). Show a profile completeness progress bar (percentage of fields filled). Save all fields via a PATCH /api/teacher/profile API endpoint. Use Laravel API Resource for response.TeacherProfileController, resources/js/components/TeacherProfile.vue1.10 Student Profile PageCreate /student/profile route (auth + role:student middleware). Simple single-page form: name (prefilled), class/grade (dropdown: Class 1 through Class 12, Undergraduate, Postgraduate), school/college name, subjects needed (multi-select same list as teacher), preferred language of instruction. Profile photo upload (same as teacher, 300x300 crop). Save via PATCH /api/student/profile. On save, show a success toast notification.StudentProfileController, resources/js/components/StudentProfile.vue1.11 DashboardsCreate three separate dashboard views: /student/dashboard, /teacher/dashboard, /admin/dashboard. Each protected by auth + role middleware. Student dashboard: show profile completeness, count of saved teachers (0 for now), upcoming bookings placeholder. Teacher dashboard: show profile completeness, whether profile is verified (show 'Pending Verification' badge if not), earnings placeholder. Admin dashboard: show total users count, pending teacher verifications count, placeholder sections for future phases. All dashboards use a shared AppLayout.vue component with a sidebar navigation.resources/js/Layouts/AppLayout.vue, Dashboard views1.12 Navigation & LayoutBuild a responsive AppLayout using Vue 3 + Tailwind CSS. Header: EduBridge logo (text-based for now), nav links that change based on user role, user avatar + name dropdown with links to Profile, Settings, Logout. Sidebar (for dashboard pages): student sidebar shows Home, Find Teachers, My Sessions, Messages. Teacher sidebar shows Home, My Profile, Sessions, Earnings, Messages. Admin sidebar shows Dashboard, Users, Verifications, Reports. Mobile: hamburger menu that slides out the sidebar. Make layout fully responsive (mobile-first).resources/js/Layouts/, resources/js/Components/Sidebar.vue1.13 Settings PageCreate /settings route available to all roles. Tabs: Account (change name, email, phone), Password (change password — requires current password), Notifications (placeholder checkboxes for future phase), Delete Account (soft delete with confirmation modal). All changes via API endpoints. Show validation errors inline.SettingsController, resources/js/Pages/Settings.vue1.14 Admin: Teacher Verification QueueAdmin can see a list of teachers with status='pending' verification. Each row shows: teacher name, registration date, subjects, a View button. View page shows all teacher profile details. Admin can click Approve (sets is_verified=true, sends welcome email) or Reject (with rejection reason text, sends rejection email). For now, teachers manually upload credentials in the bio field — file upload comes in Phase 5.AdminVerificationController, resources/js/Pages/Admin/Verifications.vue1.15 Seeders & Test DataCreate seeders: (1) RoleSeeder — create student, teacher, admin roles. (2) AdminSeeder — create one admin user: admin@edubridge.com / password: Admin@123. (3) TeacherSeeder — create 10 dummy verified teachers with complete profiles, various subjects, random ratings. (4) StudentSeeder — create 5 dummy students. Run all via DatabaseSeeder. Make sure php artisan db:seed works cleanly from scratch.database/seeders/*.php1.16 Feature TestsWrite PHPUnit Feature Tests: (1) StudentRegistrationTest — test successful registration, test duplicate email fails, test OTP verification. (2) TeacherRegistrationTest — same. (3) LoginTest — test login, test wrong password returns 422, test suspended user is blocked. (4) TeacherProfileTest — test profile update saves correctly. Use RefreshDatabase trait. All tests must pass: php artisan test.tests/Feature/*.php
Phase 1 — Deliverables Checklist
• Working Laravel 10 project with Vite + Vue 3
• All database migrations run cleanly from scratch
• Student and Teacher registration with OTP email verification
• Google OAuth login for students
• Complete teacher and student profile pages
• Role-based dashboards (student, teacher, admin)
• Admin can approve/reject teacher verification
• All seeders working, 15+ test records in DB
• All PHPUnit tests passing


PHASE 2: Core Features — Teacher Search, Filters & Real-Time Chat
Duration: Months 3–4  |  Goal: Students can find teachers and have live conversations with them
Phase 2 Overview
Phase 2 delivers the two most critical user-facing features: the ability to find the right teacher, and the ability to start a conversation. The search system must be fast, filterable, and produce useful results. The chat system must feel instant — no page refreshes, messages appearing in real time. By the end of Phase 2, a student should be able to search for a Math teacher in Hindi, view their profile, and start chatting with them.

Phase 2 — Additional Packages
• Laravel Scout + MeiliSearch (or TNTSearch) for full-text teacher search
• Pusher PHP SDK (pusher/pusher-php-server) for WebSocket broadcasting
• Laravel Echo (npm) on the frontend for real-time event listening
• pusher-js (npm) as the frontend Pusher client

Phase 2 — Task Breakdown
Task / ComponentDetailed Description for AI AgentFiles / Artifacts2.1 Teacher Search PageCreate /teachers route. Fetch all verified teachers (is_verified=true, status=active) from the API. Display as a grid of Teacher Cards. Each card shows: avatar, name, subjects (tags), languages, experience years, rating stars, hourly rate (or 'Free' badge in green), and a 'View Profile' button. Add pagination (12 per page) with a 'Load More' button. Default sort: by rating descending. This is a Vue 3 component (TeacherSearch.vue) that calls GET /api/teachers with query params.TeacherSearchController, TeacherResource, TeacherSearch.vue2.2 Search Bar & Auto-completeAdd a search bar at the top of the teachers page. As user types (debounce 300ms), call GET /api/teachers/search?q=... and show results. Use Laravel Scout with MeiliSearch (or TNTSearch for simpler setup). Index teacher_profiles with: name (from users), bio, subjects, languages. Results should match partial words (e.g., 'math' finds 'Mathematics'). Show a loading spinner while searching. If no results, show 'No teachers found. Try different keywords.'app/Scout/TeacherIndexConfigurator.php, SearchController2.3 Advanced Filter PanelAdd a collapsible filter sidebar/panel on the teachers page. Filters: Subject (multi-select checkboxes — all subjects list), Language (multi-select), Price Range (slider: Free / Under ?200 / ?200–500 / ?500+), Minimum Rating (radio: 4+, 3+, Any), Availability Day (checkboxes: Monday–Sunday), Gender preference (Any / Male / Female). When filters change, re-call GET /api/teachers with filter query params. Backend TeacherController must handle all filter params using Eloquent where/whereJsonContains clauses. Show active filter count badge on the filter button.TeacherController@index, resources/js/components/FilterPanel.vue2.4 Sort OptionsAdd a sort dropdown on the teachers listing: Relevance (default for search), Rating: High to Low, Price: Low to High, Price: High to Low, Most Experienced, Newest. Pass sort param to the API. Backend applies the sort using orderBy. 'Relevance' only applies when there is a search query (Scout ranking); otherwise default to rating desc.TeacherController@index (sort logic)2.5 Teacher Public Profile PageCreate /teachers/{id} route. Show the full teacher profile: large avatar, name, Verified badge (if is_verified), subjects tags, languages, experience, hourly rate, bio, overall star rating with total review count, list of recent reviews (latest 5), availability summary ('Available Mon, Wed, Fri evenings' — stub for now, calendar comes in Phase 3), and two CTA buttons: 'Send Message' and 'Book Session' (Book Session is disabled/grayed for now until Phase 3). This is a publicly accessible page — no login required to VIEW, but login required to send message.TeacherPublicProfileController, TeacherPublicProfile.vue2.6 Save / Bookmark TeacherStudents can save a teacher to their saved list. Add a bookmark icon on teacher cards and the teacher profile page. Clicking it calls POST /api/students/saved-teachers/{teacher_id} (save) or DELETE (remove). Create saved_teachers table: id, student_id, teacher_id, created_at (unique index on both). Show saved teachers list at /student/saved-teachers. Show a heart/bookmark toggle animation on click.SavedTeacherController, saved_teachers migration2.7 Real-Time Chat — Database SetupCreate these migrations: (1) conversations — id, created_by (user_id FK), title VARCHAR(150) nullable, is_group BOOLEAN default false, created_at, updated_at. (2) conversation_participants — id, conversation_id FK, user_id FK, joined_at, left_at nullable, unique(conversation_id, user_id). (3) messages — id, conversation_id FK, sender_id (user_id FK), body TEXT, type ENUM('text','image','file') default text, file_url VARCHAR(500) nullable, read_at TIMESTAMP nullable, created_at, softDeletes. Add models: Conversation (hasMany Messages, belongsToMany Users via conversation_participants), Message (belongsTo Conversation, belongsTo User as sender).database/migrations/, Conversation.php, Message.php2.8 Pusher + Laravel Echo SetupInstall Pusher: composer require pusher/pusher-php-server. Add to .env: BROADCAST_DRIVER=pusher, PUSHER_APP_ID, PUSHER_APP_KEY, PUSHER_APP_SECRET, PUSHER_APP_CLUSTER. In config/broadcasting.php, set pusher as default. Uncomment BroadcastServiceProvider in bootstrap/providers.php. On frontend: npm install laravel-echo pusher-js. In resources/js/bootstrap.js, initialize Echo with Pusher credentials from import.meta.env (Vite env vars). Test that broadcast works using php artisan tinker to fire a test event.config/broadcasting.php, .env, resources/js/bootstrap.js2.9 Chat API EndpointsCreate ChatController with these API routes (all auth-protected): GET /api/conversations — list all conversations for current user with last message preview and unread count. POST /api/conversations — start a new 1:1 conversation with a teacher (student sends first message). Body: { teacher_id, message }. If conversation already exists between this student and teacher, return existing conversation. GET /api/conversations/{id}/messages — get paginated messages (20 per page, cursor pagination). POST /api/conversations/{id}/messages — send a message. Body: { body, type }. Fire MessageSent broadcast event. PATCH /api/conversations/{id}/read — mark all messages in conversation as read for current user. All responses use API Resources.ChatController, MessageResource, ConversationResource2.10 Broadcast EventsCreate two broadcast events: (1) MessageSent — broadcasts on private channel private-conversation.{conversationId}. Payload: full message object (id, body, type, sender name/avatar, sent_at). Implements ShouldBroadcast. (2) UserTyping — broadcasts on presence channel presence-conversation.{conversationId}. Payload: { user_id, name }. Does NOT get stored in DB — pure real-time signal. Add channel authentication in routes/channels.php: authenticate that the current user is a participant of the conversation before allowing subscription.app/Events/MessageSent.php, UserTyping.php, routes/channels.php2.11 Chat UI — Conversation ListBuild a ChatLayout.vue component. Left panel: list of all conversations sorted by latest message. Each row: avatar, name, last message preview (truncated to 50 chars), timestamp, unread count badge (red circle). Clicking a conversation opens it in the right panel. Add a 'New Chat' button. On mount, listen to a private user channel private-user.{userId} for a NewConversation event so new conversations appear without refresh.resources/js/Pages/Chat/ChatLayout.vue, ConversationList.vue2.12 Chat UI — Message WindowBuild MessageWindow.vue. Display messages in a scrollable container. My messages: right-aligned, blue bubble. Other person's messages: left-aligned, gray bubble. Show sender avatar for received messages. Show timestamp under each message. Show read receipt tick under sent messages (single tick = sent, double tick = read_at is not null). Input area at bottom: text input (Enter to send, Shift+Enter for newline), send button, paperclip button (file upload — Phase 2 supports images only, max 5MB, store to AWS S3 or local storage/public/chat-files/). On input keydown, fire UserTyping event via Echo (throttle to once per 2 seconds). Show 'Teacher is typing...' indicator when UserTyping event received. Auto-scroll to bottom on new message. On conversation open, call the mark-as-read endpoint.resources/js/Pages/Chat/MessageWindow.vue2.13 Chat UI — Initiate from Teacher Profile'Send Message' button on teacher profile page. If student is not logged in, redirect to /login with redirect_back parameter. If logged in as student, create/open conversation via POST /api/conversations and navigate to /chat/{conversationId}. Teachers can reply but cannot initiate new conversations (they can only accept/reply).TeacherPublicProfile.vue (send message button logic)2.14 Push Notifications for ChatWhen a new message is sent, dispatch a SendChatNotification job to the queue. The job: check if the recipient has the chat window open (use presence channel online status); if not online, send an email notification using Laravel Mailable (NewMessageMail). Email shows: sender name, message preview, a 'Reply Now' button linking to /chat/{conversationId}. Use Laravel queue (database driver for now, Redis in production). Run: php artisan queue:work in background. In .env: QUEUE_CONNECTION=database. Create jobs table: php artisan queue:table.app/Jobs/SendChatNotification.php, app/Mail/NewMessageMail.php2.15 Feature Tests for Chat & SearchWrite tests: TeacherSearchTest — test filter by subject returns correct teachers, test sort by rating. ChatTest — test starting conversation creates records correctly, test sending message fires broadcast event, test mark-as-read updates read_at, test unauthorized user cannot read someone else's conversation messages.tests/Feature/TeacherSearchTest.php, ChatTest.php
Phase 2 — Deliverables Checklist
• Teacher search page with full-text search, filters, and sorting
• Individual teacher public profile pages
• Student can save/bookmark teachers
• Real-time 1:1 chat between student and teacher using Pusher WebSockets
• Typing indicators, read receipts, file/image sharing in chat
• Email notifications for new messages
• All feature tests passing


PHASE 3: Video Calls, Booking System & Payments
Duration: Months 5–6  |  Goal: Students can book sessions, pay, and join live video calls with teachers
Phase 3 Overview
Phase 3 is the commercial heart of EduBridge. It introduces the full booking lifecycle: teacher sets availability ? student books a slot ? payment is collected ? both join a video call ? payment is released. This phase must be built with careful attention to state management (booking statuses), payment security (escrow model), and video call reliability.

Phase 3 — Additional Packages
• twilio/sdk (PHP) — Twilio PHP Helper Library for video room management
• razorpay/razorpay (PHP) — Razorpay PHP SDK for payment processing
• spatie/laravel-medialibrary (optional) — for recording file management

Phase 3 — Task Breakdown
Task / ComponentDetailed Description for AI AgentFiles / Artifacts3.1 Availability & Calendar DB SetupCreate migration teacher_availability: id, teacher_id FK, day_of_week ENUM('monday','tuesday',...,'sunday') nullable, start_time TIME, end_time TIME, is_recurring BOOLEAN default true, specific_date DATE nullable (for one-off slots), is_active BOOLEAN default true, timestamps. Create migration booking_slots: id, teacher_id FK, slot_date DATE, start_time TIME, end_time TIME, is_booked BOOLEAN default false, booking_id FK nullable, timestamps. TeacherAvailability model with Teacher relationship.teacher_availability migration, booking_slots migration3.2 Teacher Availability SettingsAdd an Availability tab to the teacher dashboard. Teacher sees a weekly calendar grid (Mon–Sun). They can add time slots: pick day, start time, end time, mark as recurring (every week) or one-time (specific date). Slots are stored in teacher_availability. A scheduled command (php artisan schedule:run, daily) generates booking_slots records 4 weeks in advance from recurring availability. Show existing slots with a delete button. Validation: end_time must be after start_time, slots cannot overlap.AvailabilityController, TeacherAvailability.vue, app/Console/Commands/GenerateBookingSlots.php3.3 Booking DB & ModelCreate migration bookings: id, student_id FK, teacher_id FK, slot_id FK ? booking_slots, start_at DATETIME, end_at DATETIME, status ENUM('pending','confirmed','completed','cancelled','refunded') default pending, session_type ENUM('solo','group') default solo, subject VARCHAR(100) nullable, notes TEXT nullable (student's request note), price DECIMAL(8,2) default 0, platform_fee DECIMAL(8,2) default 0, teacher_payout DECIMAL(8,2) default 0, payment_status ENUM('unpaid','paid','held','released','refunded') default unpaid, timestamps, softDeletes. Booking model: belongsTo Student (User), Teacher (User), BookingSlot.bookings migration, Booking.php model3.4 Booking Flow — Student SideOn teacher profile page, the 'Book Session' button (now enabled) opens a booking modal. Modal flow: Step 1 — Pick a date: show a monthly calendar, highlight dates where teacher has available slots. Step 2 — Pick a time slot: show available time slots for the selected date as clickable cards (e.g., '10:00 AM – 11:00 AM'). Step 3 — Add details: subject dropdown (from teacher's subjects), notes text area (what do you need help with?), session type radio (solo/group). Step 4 — Review & Pay: show summary with teacher name, date/time, price (or FREE). If price > 0 show Razorpay payment button. If free show 'Confirm Booking' button. On confirm: call POST /api/bookings. On success, navigate to /student/bookings.BookingController@store, BookingModal.vue3.5 Razorpay Payment IntegrationInstall razorpay/razorpay. Add to .env: RAZORPAY_KEY_ID, RAZORPAY_KEY_SECRET. Payment flow: (1) Student initiates booking ? backend creates a Razorpay order via SDK: $api->order->create(['amount'=>$amountInPaise, 'currency'=>'INR', 'receipt'=>'booking_'.$bookingId]). Return order_id to frontend. (2) Frontend opens Razorpay checkout popup using razorpay JS. Student pays. (3) On payment success, frontend receives razorpay_payment_id, razorpay_order_id, razorpay_signature. (4) Frontend calls POST /api/payments/verify with these three values. Backend verifies signature using HMAC-SHA256: hash_hmac('sha256', $orderId.'|'.$paymentId, $secret) === $signature. (5) On verification success, update booking.payment_status='held', create payments record, mark booking_slot.is_booked=true, update booking.status='confirmed', send confirmation email to both parties.PaymentController, app/Services/RazorpayService.php, payments migration3.6 Payments Escrow & ReleaseCreate payments table: id, booking_id FK, payer_id FK, amount, platform_fee, teacher_payout, gateway ENUM('razorpay','free'), gateway_order_id, gateway_payment_id, status ENUM('pending','captured','held','released','refunded'), paid_at, released_at, timestamps. Platform fee = 12% of booking price. Teacher payout = 88%. On booking completion (status ? completed), dispatch a ReleasePayment job (delayed 24 hours — gives time for disputes). Job calls Razorpay transfer API to transfer teacher_payout to teacher's linked bank account (requires Razorpay Route feature). For now, simply update payment.status='released' and teacher earnings record. Create teacher_earnings table: id, teacher_id, payment_id, amount, payout_date, timestamps.ReleasePayment job, teacher_earnings migration, PaymentController3.7 Booking Status ManagementImplement full booking state machine. Allowed transitions: pending ? confirmed (after payment), confirmed ? completed (after session end), confirmed ? cancelled (by student 2+ hours before, or teacher 1+ hours before), confirmed ? no-show (if session not joined — via scheduled job). Cancellation logic: if student cancels more than 2 hours before: full refund. If student cancels within 2 hours: no refund. If teacher cancels: full refund always. Refund via Razorpay refund API. Notify both parties on every status change via email and in-app notification.BookingController, app/Services/BookingService.php3.8 Twilio Video IntegrationInstall twilio/sdk. Add to .env: TWILIO_ACCOUNT_SID, TWILIO_API_KEY, TWILIO_API_SECRET. Create VideoSessionController. Endpoint POST /api/video-sessions/{bookingId}/token: verify the requesting user is the student or teacher on that booking, booking status must be confirmed, generate a Twilio AccessToken with a VideoGrant for room name format edubridge-booking-{bookingId}, return token + room name. Create video_sessions table: id, booking_id FK, room_name, twilio_room_sid nullable, started_at nullable, ended_at nullable, recording_url nullable, timestamps.VideoSessionController, video_sessions migration, app/Services/TwilioService.php3.9 Video Call UICreate /session/{bookingId} page. This page is only accessible 15 minutes before the scheduled start time (show countdown if too early). On page load, fetch Twilio token from the API. Use @twilio/video npm package to connect to the Twilio room. UI layout: main video area (remote participant full screen), self-video (small overlay bottom-right). Controls bar: Mute/Unmute microphone button, Camera On/Off button, Screen Share button (uses getDisplayMedia), End Call button, Chat button (opens side panel with in-session text chat). Session timer visible at top. On both participants joining: mark video_session.started_at = now(). When teacher clicks End Call: mark started_at/ended_at, trigger booking status ? completed (if session was at least 5 minutes long), dispatch payment release job.resources/js/Pages/VideoSession.vue, VideoSessionController3.10 Booking Management PagesStudent /student/bookings: show tabs — Upcoming, Completed, Cancelled. Each booking shows: teacher avatar, name, subject, date/time, status badge, price, and action buttons (Join Session / Cancel / Reschedule). Teacher /teacher/sessions: same concept but from teacher's perspective. Add earnings tab showing total earned, pending payout, payout history. Admin /admin/bookings: all bookings across the platform, filterable by status, date, user. Ability to manually change status and trigger refunds for dispute resolution.BookingController, resources/js/Pages/Student/Bookings.vue, Teacher/Sessions.vue3.11 Email & SMS NotificationsSend emails for: booking confirmation (to student and teacher), 24-hour reminder before session, 1-hour reminder, booking cancellation with refund details, session completed with prompt to leave review. For SMS: use Twilio SMS API. Send SMS for: booking confirmation, 24-hour reminder, session about to start (15-min warning). SMS is especially important for teachers who may not check email regularly. Create a NotificationService class that decides which channel to use based on user preferences (store preferences in user_notification_preferences table).app/Services/NotificationService.php, app/Mail/, app/Notifications/3.12 Ratings & ReviewsCreate reviews table: id, booking_id FK unique, reviewer_id FK, reviewee_id FK, rating TINYINT (1–5), comment TEXT nullable, is_visible BOOLEAN default true, created_at. After session completion, student receives an in-app prompt and email to rate the teacher. Create /reviews/{bookingId} page. Rating UI: 5-star click selector + optional text comment. On submit: POST /api/reviews. Backend: validate booking status is completed, reviewer is the student, no review exists yet. After saving: update teacher_profiles.rating_avg and total_reviews (use DB average query). Reviews visible on teacher public profile.ReviewController, reviews migration, resources/js/Pages/Reviews/LeaveReview.vue3.13 Feature Tests — Payments & BookingWrite tests: BookingTest — test slot is marked booked after booking, test double-booking same slot fails. PaymentTest — test Razorpay signature verification, test free session creates booking without payment. CancellationTest — test early cancellation triggers full refund, test late cancellation does not refund. VideoSessionTest — test token endpoint returns valid structure, test unauthorized user cannot get token.tests/Feature/BookingTest.php, PaymentTest.php
Phase 3 — Deliverables Checklist
• Teacher availability calendar — recurring and one-off slots
• Full booking flow: pick slot ? review ? pay ? confirmed
• Razorpay payment with escrow and automatic payout release
• Twilio video call with mute, camera, screen share, and chat
• Booking management pages for student, teacher, and admin
• Post-session ratings and reviews
• Email + SMS notifications for all booking lifecycle events


PHASE 4: Group Features — Group Chat, Group Video & Whiteboard
Duration: Month 7  |  Goal: Teachers can run virtual classrooms for multiple students at once
Phase 4 Overview
Phase 4 adds the classroom dimension to EduBridge — moving beyond 1:1 tutoring to allow a teacher to create a persistent group (like a class), add multiple students, broadcast announcements, and run live group video sessions with up to 30 students. The whiteboard tool makes math and diagram explanations possible.

Phase 4 — Task Breakdown
Task / ComponentDetailed Description for AI AgentFiles / Artifacts4.1 Group / Class DB SetupConversations table already supports groups (is_group=true, title field). Extend it: add subject VARCHAR(100), description TEXT nullable, max_students INT default 30, teacher_id FK. Create class_members table (extends conversation_participants with extra fields): id, conversation_id FK, user_id FK, role ENUM('teacher','student') default student, joined_at, left_at nullable, is_muted BOOLEAN default false. Teacher is automatically added as the admin of the group.Add columns to conversations, class_members migration4.2 Teacher Creates a Class GroupOn teacher dashboard, add a 'Create Class' button. Form: class name (e.g., 'Class 12 Math — Batch A'), subject, description, optional max students (default 30). On create: save conversation with is_group=true, teacher_id, add teacher as participant with role=teacher. Return group invite link: /join/{groupCode} where groupCode is a unique 8-char code stored on the conversation.GroupController@create, CreateGroup.vue4.3 Students Join a GroupTwo ways to join: (1) Teacher shares the /join/{groupCode} link. Student visits link, sees group details, clicks 'Join Class'. (2) Teacher adds student by their registered email (student must already have an account). On join: add to class_members. Enforce max_students limit. Send welcome notification to student.GroupController@join, GroupController@addMember4.4 Group Chat UIExtend the existing ChatLayout to handle group conversations. Group chat differences from 1:1: show group name + member count in header. Show sender name above each message bubble (not just avatar). Teacher messages have a 'Teacher' label badge. Teacher has admin controls: right-click/long-press a message to delete it. Teacher can mute a student (is_muted=true, muted student's messages show a warning). Only teacher can add/remove members (not students). Broadcast channel for group: private-conversation.{conversationId} (same as 1:1 — participants are already authenticated).GroupChat.vue extends MessageWindow.vue4.5 Group AnnouncementsTeacher can post an Announcement (special message type ENUM addition: 'announcement'). Announcement messages display differently: full-width yellow banner with a megaphone icon, pinned to the top of the chat. Only one announcement pinned at a time (new announcement replaces old pin). Students receive a push notification for announcements even if chat is closed.Add 'announcement' type to messages, AnnouncementBanner.vue4.6 Group Video SessionExtend VideoSessionController to support group bookings. For group sessions, the Twilio room supports up to 50 participants (use Group Room type in Twilio, not Peer-to-Peer). Add a 'Start Group Session' button on the teacher's group page. Students in the group see a 'Join Session' button when teacher starts a session (push notification sent). In the group video UI, add a Participants panel showing all connected students with mute status. Teacher-only controls: Mute All, Remove Participant. Students see a Raise Hand button — when clicked, their name appears in teacher's raise-hand queue.VideoSession.vue (extend for group), VideoSessionController4.7 Interactive WhiteboardIntegrate a shared whiteboard into the video session page. Use the Excalidraw React component (npm install @excalidraw/excalidraw) embedded in the Vue session page via a React island or as an iframe. The whiteboard state is shared via Pusher: when teacher draws, fire a WhiteboardUpdate broadcast event with the Excalidraw element data. All participants receive the update and re-render. Only teacher can draw by default. Teacher can give draw permission to a student. Add a toggle button to show/hide the whiteboard (overlaid on the video). Support: freehand draw, shapes (rectangle, circle, arrow), text tool, eraser, color picker, undo/redo.Whiteboard.vue, app/Events/WhiteboardUpdate.php4.8 Session RecordingAdd a 'Record Session' toggle on the video page (teacher only, requires student consent banner shown to all). When enabled, start Twilio Composition recording on the room. On session end, Twilio sends a webhook to POST /api/webhooks/twilio/recording-complete with the recording SID. Fetch the recording MP4 from Twilio, store to AWS S3 (or local for dev), update video_sessions.recording_url. Students and teacher can access the recording at /recordings/{sessionId} (auth-required, only session participants). Recording expires after 90 days (or indefinitely for Premium accounts).TwilioWebhookController, recording download page4.9 Feature Tests — Group FeaturesTests: GroupTest — test group creation, test max members limit enforced, test non-member cannot access group chat messages. WhiteboardTest — test whiteboard event broadcast structure. RecordingTest — test webhook creates recording URL.tests/Feature/GroupTest.phpPhase 4 — Deliverables Checklist
• Teacher can create class groups, generate invite links, add/remove students
• Group chat with announcements, member list, teacher moderation tools
• Group video sessions (up to 30 students) with raise hand feature
• Collaborative whiteboard using Excalidraw, synced via Pusher
• Optional session recording stored to S3 with access control


PHASE 5: Admin Panel, Credential Verification & Analytics
Duration: Month 8  |  Goal: Full admin control, credential upload system, and platform analytics
Phase 5 Overview
Phase 5 builds the operational backbone. The admin panel is the nerve center for managing users, verifying teachers, moderating content, and monitoring platform health. Teacher credential upload system allows proper document verification. Analytics give admins visibility into key metrics.

Phase 5 — Task Breakdown
Task / ComponentDetailed Description for AI AgentFiles / Artifacts5.1 Admin Panel LayoutCreate a completely separate admin section at /admin/* routes. Use a different layout (AdminLayout.vue) with a dark sidebar. Sidebar links: Dashboard, Users (Students, Teachers), Verifications, Bookings, Reports & Disputes, Reviews, Announcements, Analytics, Settings. All /admin routes protected by auth + role:admin middleware. Admin panel should be visually distinct from the main app (use a dark blue/gray theme).resources/js/Layouts/AdminLayout.vue, AdminMiddleware5.2 Teacher Credential Upload SystemCreate teacher_documents table: id, teacher_id FK, type ENUM('degree','service_record','id_proof','other'), file_path, original_filename, status ENUM('pending','approved','rejected'), rejection_reason TEXT nullable, reviewed_by (admin user_id FK) nullable, reviewed_at nullable, uploaded_at. On teacher profile, add a Documents tab: teacher uploads PDF/JPG files for each document type (max 10MB each, max 3 files). Store to private S3 bucket (not public). In admin verification queue: show each teacher's uploaded documents with a View button (generates a signed S3 URL, expires in 5 minutes). Admin can approve or reject each document individually. Teacher is fully verified only when at least one degree AND one service_record are approved.TeacherDocumentController, teacher_documents migration, S3 signed URL generation5.3 User ManagementAdmin /admin/users: tabbed view of Students and Teachers. List view with search by name/email, filter by status (active/suspended/pending). Each row shows: avatar, name, email, registration date, status badge, action menu (View Profile, Suspend, Activate, Delete). Suspend action sets user.status='suspended' — suspended users get 'Your account is suspended' message on login. Bulk actions: select multiple users ? bulk suspend or bulk delete. Export users list as CSV using a background job.AdminUserController, resources/js/Pages/Admin/Users.vue5.4 Content Moderation — ReportsCreate reports table: id, reporter_id FK, reported_user_id FK nullable, reported_message_id FK nullable, reported_review_id FK nullable, type ENUM('message','review','profile','other'), reason TEXT, status ENUM('pending','reviewed','dismissed','action_taken'), admin_notes TEXT nullable, resolved_by FK nullable, timestamps. Add a 'Report' option: (1) in chat — right-click/long-press message ? Report, (2) on teacher profile ? Report Teacher. Admin /admin/reports: list all pending reports. Admin can view reported content, see reporter context, and take action: warn user, delete content, suspend user, or dismiss report.ReportController, reports migration, Admin/Reports.vue5.5 Review ModerationAdmin /admin/reviews: list all reviews with filter by is_visible. Each row shows: student name, teacher name, rating, comment, date, visibility toggle. Admin can toggle is_visible to hide a review from public profile. Add fake review detection: flag reviews from accounts created less than 24 hours before the review, or reviews with same IP as the teacher. Show a 'Suspicious' badge on flagged reviews for admin awareness.Admin/Reviews.vue, review moderation logic5.6 Analytics DashboardAdmin analytics page with date range picker (today / 7 days / 30 days / 90 days / custom). Metrics panels: Total Users (students + teachers), New Registrations (chart by day), Active Sessions Today, Sessions Completed This Month, Total Revenue, Platform Fees Collected, Top 10 Teachers by Sessions, Top Subjects by Booking Count, User Retention (registered vs active last 7 days), Cancellation Rate. Use Chart.js (already available in Vue ecosystem) for bar and line charts. All data comes from API endpoints that run optimized DB queries. Cache these queries in Redis for 15 minutes.AnalyticsController, Admin/Analytics.vue, Chart.js components5.7 Platform-Wide AnnouncementsAdmin can create a platform-wide announcement: title, message body, target audience (All / Students Only / Teachers Only), display type (banner on dashboard / email blast / both), start_date, end_date. Banners are shown on user dashboards as dismissable alerts. Email blast dispatches bulk mail job using Laravel queue with rate limiting (100 emails/minute to avoid SMTP limits). Create announcements table: id, title, message, target_role, type, is_active, starts_at, ends_at, created_by, timestamps.AnnouncementController, announcements migration, BulkEmailJob5.8 Dispute ResolutionAdmin /admin/disputes: list cancelled bookings with disputed payment status. Admin can see the booking timeline (all status changes with timestamps), the conversation between student and teacher, and payment details. Actions: Approve Full Refund (triggers Razorpay refund API), Approve Partial Refund (enter custom amount), Release Payment to Teacher, Mark Dispute Closed. All actions logged to an audit_log table: id, admin_id, action, entity_type, entity_id, details JSON, created_at.DisputeController, audit_log migration5.9 Feature Tests — AdminTests: AdminVerificationTest — test credential approval changes is_verified, test rejection sends email. AdminSuspendTest — test suspended user cannot login. AnalyticsTest — test analytics endpoints return correct structure (not actual values). AnnouncementTest — test announcement shows on correct user dashboards.tests/Feature/Admin/*Test.phpPhase 5 — Deliverables Checklist
• Full admin panel with separate layout and complete user management
• Secure document upload and step-by-step credential verification workflow
• Content moderation system for messages, reviews, and profiles
• Analytics dashboard with charts for all key platform metrics
• Platform-wide announcement system with email blast
• Dispute resolution with full audit log


PHASE 6: Polish, PWA, Performance, Security & Beta Testing
Duration: Month 9  |  Goal: Production-ready app with PWA support, security hardening, and real user testing
Phase 6 Overview
Phase 6 takes the fully-featured app and makes it production-grade. This means PWA conversion for offline capability, performance optimization so pages load fast on mobile networks, security hardening to protect student data, accessibility improvements for elderly teachers, and beta testing with real users.

Phase 6 — Task Breakdown
Task / ComponentDetailed Description for AI AgentFiles / Artifacts6.1 Progressive Web App (PWA)Convert EduBridge to a PWA. Create a Web App Manifest (manifest.json): name=EduBridge, short_name=EduBridge, theme_color=#1E5799, background_color=#FFFFFF, display=standalone, icons (192x192 and 512x512 PNG app icons). Register a Service Worker (sw.js) using Workbox (npm install workbox-webpack-plugin): cache static assets (JS, CSS, fonts) for offline. Cache the teacher search page and user dashboard for offline viewing. Show an 'Add to Home Screen' prompt on mobile. PWA must score 90+ on Lighthouse PWA audit.public/manifest.json, public/sw.js, Vite PWA plugin6.2 Performance Optimization — BackendAudit all database queries using Laravel Debugbar/Telescope. Fix all N+1 query problems by adding eager loading (with()) to all list queries. Add database indexes: teacher_profiles (is_verified, rating_avg), bookings (student_id, teacher_id, status, start_at), messages (conversation_id, created_at), reviews (reviewee_id). Cache frequently accessed data in Redis: teacher profile (cache for 5 minutes, invalidate on update), teacher search results (cache for 2 minutes per unique filter combination). Add query caching for analytics endpoints (15 minutes). Enable OPcache on PHP. Target: all API endpoints respond in under 200ms on average.Database index migrations, app/Services/CacheService.php6.3 Performance Optimization — FrontendEnable Vite code splitting: each route loads only its own JS chunk (lazy import Vue pages with defineAsyncComponent). Enable Gzip/Brotli compression via Cloudflare or Nginx. Optimize all images: convert teacher avatars to WebP format, lazy-load images below the fold (native loading='lazy'). Preload critical fonts. Add skeleton loading screens for teacher search, chat, and dashboard (show animated gray placeholders while data loads). Target: Lighthouse Performance score 85+ on mobile.vite.config.js, LazyLoad directives, SkeletonLoader.vue6.4 Accessibility for Elderly TeachersSince teachers can be 55–75 years old: increase base font size in teacher-facing pages to 16px (not 14px). Ensure all clickable elements are minimum 44x44px touch target. Add high-contrast mode toggle (stored in user settings, affects CSS variables). All form labels must be explicit (not placeholder-only). Add aria-label attributes to all icon buttons. Test with keyboard navigation (Tab order must be logical). Add a simple onboarding walkthrough for new teachers using a tooltip-based tour library (driver.js). First login shows a 4-step tour: Here is your profile, Here is how to set availability, Here is how to respond to messages, Here is how to join a session.AccessibilityStyles.vue, Onboarding tour with driver.js6.5 Security HardeningRun a security audit checklist: (1) HTTPS enforced — set FORCE_HTTPS=true, redirect all HTTP to HTTPS in Laravel. (2) Rate limiting — 5 failed logins per minute per IP (use Laravel RateLimiter). (3) Rate limit chat messages: max 30 messages per minute per user. (4) All file uploads: validate MIME type server-side (not just extension), scan with ClamAV if available, reject executable files. (5) Add Content-Security-Policy header in HTTP middleware. (6) Add X-Frame-Options: DENY header. (7) Use parameterized queries everywhere (Eloquent already handles this — just verify no raw DB::select with interpolation). (8) Admin accounts require 2FA: implement TOTP using pragmarx/google2fa package. (9) Log all admin actions to audit_log (already done in Phase 5). (10) Implement account lockout after 10 failed login attempts.app/Http/Middleware/SecurityHeaders.php, 2FA setup6.6 Mobile Responsiveness AuditTest every page on iPhone SE (375px), iPhone 14 (390px), Samsung Galaxy (360px), and iPad (768px). Fix issues: teacher search filter panel must work as a slide-up bottom sheet on mobile. Chat must fill full screen on mobile with keyboard not covering the input area (use CSS env(keyboard-inset-height)). Video session controls must be large enough to tap with a thumb. Navigation must work with thumb reach in mind (bottom navigation bar on mobile instead of top nav).Responsive CSS fixes, MobileNav.vue6.7 Notification PreferencesBuild a proper notification preferences system. Create user_notification_preferences table: id, user_id FK, type VARCHAR(100), email BOOLEAN default true, sms BOOLEAN default true, push BOOLEAN default true. Types: new_message, booking_confirmed, session_reminder, review_received, account_changes. Settings UI at /settings ? Notifications tab. Show toggle switches for each notification type and channel. All notification-sending code must check preferences before sending.NotificationPreferenceController, notification_preferences migration6.8 Beta Testing SetupPrepare for beta testing: (1) Create a staging environment (separate DB, separate .env, MAIL_MAILTRAP=true). (2) Add a feedback widget to all pages (floating button) — clicking opens a modal with: Type (Bug / Feature Request / General), Description, optional screenshot upload. Feedback stored in feedbacks table and emailed to admin. (3) Add Google Analytics 4 (gtag.js) for usage tracking. (4) Create a beta tester invite system: admin generates invite codes, users register with invite code during beta (add invite_code column to users). (5) Set up error tracking with Sentry (composer require sentry/sentry-laravel).feedbacks table, FeedbackWidget.vue, Sentry integration6.9 End-to-End TestsWrite Laravel Dusk browser automation tests for critical user journeys: (1) StudentSignupAndFindTeacher — register ? verify email ? search for Math teacher ? view profile ? send message. (2) TeacherCompleteProfileAndAcceptBooking — login ? complete profile ? set availability ? receive booking notification ? accept. (3) FullSessionFlow — student books ? pays ? both join video ? session ends ? student rates teacher. Run: php artisan dusk.tests/Browser/*Test.phpPhase 6 — Deliverables Checklist
• PWA with offline caching, installable on mobile home screen
• All N+1 queries fixed, Redis caching, 85+ Lighthouse score
• Security hardened: 2FA for admins, rate limiting, CSP headers, file upload scanning
• Fully responsive on all mobile screen sizes
• Elderly-friendly design: large fonts, high-contrast mode, onboarding tour
• Beta testing infrastructure: feedback widget, Sentry, staging environment, invite codes
• Dusk end-to-end tests for all 3 critical user journeys


PHASE 7: Public Launch, Scaling Infrastructure & Post-Launch
Duration: Month 10+  |  Goal: Launch the platform publicly and prepare for growth
Phase 7 Overview
Phase 7 is about going live and being ready to handle real traffic. This includes setting up proper cloud infrastructure, configuring the production environment, implementing horizontal scaling capabilities, and planning for the first wave of users. Post-launch monitoring and rapid iteration are the focus.

Phase 7 — Task Breakdown
Task / ComponentDetailed Description for AI AgentFiles / Artifacts7.1 DockerizationCreate a Docker setup for the application. Files needed: Dockerfile (PHP 8.2-fpm base, install extensions: pdo_mysql, redis, gd, zip, pcntl), docker-compose.yml (services: app, nginx, mysql, redis, meilisearch), nginx.conf (proxy to PHP-FPM, gzip, static asset caching headers), .dockerignore. The docker-compose.yml is for DEVELOPMENT only — production uses individual managed services. Test: docker-compose up --build and confirm all services start. Include volume mounts for storage/ and .env.Dockerfile, docker-compose.yml, nginx.conf7.2 Production Server Setup (AWS / DigitalOcean)Set up production on DigitalOcean (recommended for simplicity and cost). Droplet: 4 vCPU, 8GB RAM ($48/month starter). Install: Ubuntu 22.04, Nginx, PHP 8.2-FPM, MySQL 8 (or use DigitalOcean Managed Database), Redis (or DigitalOcean Managed Redis). Use Laravel Forge or Ploi.io for server management (automated deploy, SSL, queue workers). Connect domain name, configure DNS. SSL via Let's Encrypt (Forge handles this automatically). Set up a separate queue worker process (Supervisor manages it, 4 worker processes). Set up Laravel Scheduler: add * * * * * cd /var/www/edubridge && php artisan schedule:run to crontab.Server provisioning scripts, Forge/Ploi configuration7.3 File Storage — AWS S3Set up AWS S3 bucket: edubridge-production. Configure bucket: block all public access (private bucket). Set up two folders: avatars/ (profile photos — these use CloudFront CDN for fast delivery), private/ (documents and session recordings — access via signed URLs only). In .env: FILESYSTEM_DISK=s3, AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_DEFAULT_REGION, AWS_BUCKET. Update all file storage calls to use Storage::disk('s3'). Set up AWS CloudFront CDN in front of the avatars/ folder for fast image delivery globally..env S3 config, config/filesystems.php, CloudFront setup7.4 CI/CD PipelineSet up GitHub Actions CI/CD pipeline. On push to main branch: (1) Run php artisan test (all PHPUnit tests must pass). (2) Run npm run build (Vite build). (3) If tests pass, trigger Forge deploy webhook (Forge pulls latest code, runs composer install --no-dev, npm ci, php artisan migrate --force, php artisan config:cache, php artisan route:cache, php artisan view:cache, php artisan queue:restart). GitHub Actions file: .github/workflows/deploy.yml. Also run tests on pull requests to main (but don't deploy)..github/workflows/deploy.yml, Forge deploy webhook7.5 Horizontal Scaling PreparationPrepare the app to run on multiple servers: (1) Sessions: switch SESSION_DRIVER=redis (so sessions work across multiple servers). (2) Cache: CACHE_DRIVER=redis. (3) Queue: QUEUE_CONNECTION=redis. (4) File storage: already on S3. (5) Remove any reliance on local filesystem for state. (6) Test by running two separate PHP-FPM processes and confirming sessions persist. Future scaling: add a load balancer (DigitalOcean Load Balancer) and a second web server pointing to the same RDS/managed database and Redis..env driver changes, Redis session driver7.6 Monitoring & AlertingSet up monitoring stack: (1) Sentry (already installed in Phase 6) — configure alert rules: email admin when error rate exceeds 5/minute, when a new error type is first seen. (2) Laravel Telescope — disable in production (TELESCOPE_ENABLED=false). (3) DigitalOcean Monitoring — set CPU alert (email if CPU > 80% for 5 minutes), memory alert. (4) Uptime monitoring — use UptimeRobot (free tier): monitor / and /api/health (create a health check endpoint that pings DB and Redis). (5) Log aggregation — send Laravel logs to a managed log service (Papertrail or Logtail).app/Http/Controllers/HealthCheckController.php, Sentry alerts7.7 Launch Checklist & Go-LiveFinal pre-launch checklist: run php artisan config:cache, route:cache, view:cache, event:cache. Set APP_DEBUG=false, APP_ENV=production. Run php artisan optimize. Confirm all environment variables are set. Test payment flow end-to-end in production with a ?1 test booking. Test video session with 2 devices. Test email delivery. Announce beta launch: create a landing page at /landing (no auth required) explaining EduBridge's mission, 'Sign Up as Teacher' / 'Sign Up as Student' CTAs, screenshots of the app. SEO basics: set meta tags, Open Graph tags for social sharing.Final .env production settings, /landing page7.8 Post-Launch Iteration PlanAfter launch, prioritize based on user feedback: (1) Mobile apps — React Native app (reuses existing APIs, just new frontend). (2) AI matching — collect booking and search data for 3 months, then build a simple collaborative filtering recommendation model. (3) Multilingual UI — use Laravel localization (lang/ files) to add Hindi translations. (4) Parent dashboard — add a parent role that can monitor a student's sessions and progress. (5) Study material uploads — teachers can upload PDFs/notes that enrolled students can download. Track everything with Google Analytics and Sentry — fix issues fast.Roadmap issues on GitHubPhase 7 — Deliverables Checklist
• Dockerized application running in production on DigitalOcean
• Automated CI/CD pipeline via GitHub Actions + Laravel Forge
• S3 file storage with CloudFront CDN for avatars
• All state (sessions, cache, queues) on Redis for horizontal scalability
• Monitoring: Sentry, UptimeRobot, DigitalOcean alerts, health check endpoint
• Landing page live with SEO basics
• App successfully serving real users


Complete Phase Summary

PhaseTitleDurationKey OutputMain Tech1FoundationMonths 1–2Auth, profiles, dashboards, admin verificationLaravel Breeze, Spatie, Seeders2Search & ChatMonths 3–4Teacher search with filters, real-time 1:1 chatLaravel Scout, Pusher, Echo3Video & PaymentsMonths 5–6Booking calendar, video calls, Razorpay paymentsTwilio Video, Razorpay, Queues4Group FeaturesMonth 7Class groups, group video, whiteboard, recordingsPusher (group), Excalidraw, Twilio Composition5Admin & AnalyticsMonth 8Full admin panel, credential upload, analyticsChart.js, S3 signed URLs, Audit Log6Polish & BetaMonth 9PWA, performance, security, accessibility, testingWorkbox, Dusk, Sentry, 2FA7Launch & ScaleMonth 10+Production deploy, CI/CD, monitoring, landing pageDocker, GitHub Actions, Forge, S3 CDN
EduBridge — Connecting Retired Teachers with Students
"Retired teachers have taught for decades. EduBridge gives them the stage to teach for decades more."
